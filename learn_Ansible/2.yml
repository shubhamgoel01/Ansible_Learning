---   # To install HTTPD  To Uninstall yum remove httpd -y

  - hosts: all
    user: root
    become: yes
    gather_facts: yes
    tasks:
            - name: install httpd
              action: yum name=httpd state=installed





